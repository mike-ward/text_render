---
phase: 32-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "v test passes with zero failures"
    - "stress_demo scrolls 30+ seconds without flicker/blank/delay"
    - "editor_demo text entry, selection, scrolling work clean"
    - "atlas_debug renders glyph atlas without corruption"
  artifacts: []
  key_links: []
---

<objective>
Verify all demos and tests are regression-free after Phase 31
vmemcpy fix.

Purpose: Final gate for v1.7 stabilization milestone — confirm
the fix resolved all v1.6 regressions without introducing new ones.
Output: Verification results; no code changes.
</objective>

<execution_context>
@/Users/mike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-fix/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <action>
Run `v test .` from the project root. All 6 test files must pass
with zero failures:
- _api_test.v
- _font_height_test.v
- _font_resource_test.v
- _layout_test.v
- _text_height_test.v
- _validation_test.v

If any test fails, report the failure details and STOP — do not
proceed to Task 2.
  </action>
  <verify>`v test .` exits 0 with all tests passing</verify>
  <done>Zero test failures across all 6 test files</done>
</task>

<task type="auto">
  <name>Task 2: Compile all three demo targets</name>
  <files></files>
  <action>
Compile each demo to verify no build errors:

1. `v -check-syntax examples/stress_demo.v`
2. `v -check-syntax examples/editor_demo.v`
3. `v -check-syntax examples/atlas_debug.v`

All three must compile cleanly. If any fails, report error and
STOP.
  </action>
  <verify>All three commands exit 0</verify>
  <done>stress_demo, editor_demo, atlas_debug all compile</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all demos</name>
  <what-built>
Phase 31 vmemcpy fix applied. Tests pass and demos compile.
Now need visual confirmation of all three demos.
  </what-built>
  <how-to-verify>
**VRFY-01 — stress_demo (30s scroll test):**
1. Run: `v run examples/stress_demo.v`
2. Scroll continuously up and down for 30+ seconds
3. Confirm: NO flickering, NO blank regions, NO visible delays
4. Close the window

**VRFY-02 — editor_demo (edit + select + scroll):**
1. Run: `v run examples/editor_demo.v`
2. Type several lines of text
3. Select text with shift+arrow or mouse drag
4. Scroll if content overflows
5. Confirm: NO visual artifacts, text renders correctly

**VRFY-03 — atlas_debug (atlas rendering):**
1. Run: `v run examples/atlas_debug.v`
2. Observe the glyph atlas visualization
3. Confirm: NO corruption, glyphs render cleanly
  </how-to-verify>
  <resume-signal>
Type "approved" if all 3 demos pass, or describe which demo(s)
have issues.
  </resume-signal>
</task>

</tasks>

<verification>
- `v test .` passes (VRFY-04)
- All demos compile without errors
- User confirms stress_demo clean for 30s (VRFY-01)
- User confirms editor_demo functional (VRFY-02)
- User confirms atlas_debug clean (VRFY-03)
</verification>

<success_criteria>
All 4 VRFY requirements satisfied: tests pass, stress_demo clean,
editor_demo clean, atlas_debug clean. v1.7 milestone ready to ship.
</success_criteria>

<output>
After completion, create
`.planning/phases/32-verification/32-01-SUMMARY.md`
</output>
